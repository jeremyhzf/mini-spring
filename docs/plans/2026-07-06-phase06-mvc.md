# 阶段6 - MVC框架实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标:** 实现Web MVC框架，支持HTTP请求处理、控制器映射、视图渲染

**架构:** 前端控制器模式 + 处理器映射链 + 视图解析

**技术栈:** Java 17, JUnit 5, Maven（无外部依赖，使用Servlet API）

---

## 前置准备

### Task 0: 创建MVC相关包结构

**Files:**
- Create: `src/main/java/com/minispring/web/`
- Create: `src/main/java/com/minispring/web/servlet/`
- Create: `src/main/java/com/minispring/web/controller/`
- Create: `src/main/java/com/minispring/web/view/`
- Create: `src/main/java/com/minispring/web/annotation/`
- Create: `src/test/java/com/minispring/web/`

**Step 1: 创建目录结构**

Run: `mkdir -p src/main/java/com/minispring/web/servlet src/main/java/com/minispring/web/controller src/main/java/com/minispring/web/view src/main/java/com/minispring/web/annotation src/test/java/com/minispring/web`

**Step 2: 添加Servlet API依赖**

修改 `pom.xml`，添加Servlet API：

```xml
<dependency>
    <groupId>javax.servlet</groupId>
    <artifactId>javax.servlet-api</artifactId>
    <version>4.0.1</version>
    <provided>provided</provided>
</dependency>
```

**Step 3: 提交**

```bash
git add pom.xml
git commit -m "feat: 添加Servlet API依赖"
git add src/main/java/com/minispring/web/
git commit -m "feat: 创建MVC相关包结构"
```

---

## 核心接口定义

### Task 1: 定义MVC核心接口

**Files:**
- Create: `src/main/java/com/minispring/web/servlet/HttpServlet.java`
- Create: `src/main/java/com/minispring/web/servlet/HttpServletRequest.java`
- Create: `src/main/java/com/minispring/web/servlet/HttpServletResponse.java`
- Create: `src/main/java/com/minispring/web/controller/Controller.java`
- Create: `src/main/java/com/minispring/web/ModelAndView.java`

**Step 1: 创建HttpServlet接口**

```java
package com.minispring.web.servlet;

/**
 * HTTP Servlet接口
 * 简化的Servlet API
 */
public interface HttpServlet {
    
    /**
     * 处理GET请求
     */
    void doGet(HttpServletRequest request, HttpServletResponse response) throws Exception;
    
    /**
     * 处理POST请求
     */
    void doPost(HttpServletRequest request, HttpServletResponse response) throws Exception;
}
```

**Step 2: 创建HttpServletRequest接口**

```java
package com.minispring.web.servlet;

import java.util.Map;

/**
 * HTTP请求接口
 */
public interface HttpServletRequest {
    
    /**
     * 获取请求方法
     */
    String getMethod();
    
    /**
     * 获取请求URI
     */
    String getRequestURI();
    
    /**
     * 获取请求参数
     */
    String getParameter(String name);
    
    /**
     * 获取所有请求参数
     */
    Map<String, String[]> getParameterMap();
    
    /**
     * 设置请求属性
     */
    void setAttribute(String name, Object value);
    
    /**
     * 获取请求属性
     */
    Object getAttribute(String name);
}
```

**Step 3: 创建HttpServletResponse接口**

```java
package com.minispring.web.servlet;

/**
 * HTTP响应接口
 */
public interface HttpServletResponse {
    
    /**
     * 设置内容类型
     */
    void setContentType(String type);
    
    /**
     * 获取写入器
     */
    java.io.PrintWriter getWriter() throws java.io.IOException;
}
```

**Step 4: 创建Controller接口**

```java
package com.minispring.web.controller;

import com.minispring.web.servlet.HttpServletRequest;
import com.minispring.web.servlet.HttpServletResponse;
import com.minispring.web.ModelAndView;

/**
 * 控制器接口
 */
@FunctionalInterface
public interface Controller {
    
    /**
     * 处理请求
     *
     * @return ModelAndView 包含模型和视图信息
     */
    ModelAndView handleRequest(HttpServletRequest request, HttpServletResponse response) throws Exception;
}
```

**Step 5: 创建ModelAndView类**

```java
package com.minispring.web;

import java.util.HashMap;
import java.util.Map;

/**
 * 模型和视图容器
 */
public class ModelAndView {
    
    private Object view;
    private Map<String, Object> model = new HashMap<>();
    
    public ModelAndView() {}
    
    public ModelAndView(Object view) {
        this.view = view;
    }
    
    public ModelAndView(Object view, Map<String, Object> model) {
        this.view = view;
        this.model = model;
    }
    
    /**
     * 设置视图
     */
    public void setView(Object view) {
        this.view = view;
    }
    
    /**
     * 获取视图
     */
    public Object getView() {
        return view;
    }
    
    /**
     * 设置视图名称
     */
    public void setViewName(String viewName) {
        this.view = viewName;
    }
    
    /**
     * 获取视图名称
     */
    public String getViewName() {
        return view != null ? view.toString() : null;
    }
    
    /**
     * 添加模型数据
     */
    public void addObject(String key, Object value) {
        this.model.put(key, value);
    }
    
    /**
     * 获取所有模型数据
     */
    public Map<String, Object> getModel() {
        return model;
    }
    
    /**
     * 判断是否有视图
     */
    public hasView() {
        return view != null;
    }
}
```

**Step 6: 提交**

```bash
git add src/main/java/com/minispring/web/
git commit -m "feat: 定义MVC核心接口"
```

---

## 请求映射注解

### Task 2: 定义请求映射注解

**Files:**
- Create: `src/main/java/com/minispring/web/annotation/RequestMapping.java`
- Create: `src/main/java/com/minispring/web/annotation/GetMapping.java`
- Create: `src/main/java/com/minispring/web/annotation/PostMapping.java`
- Create: `src/main/java/com/minispring/web/annotation/RequestParam.java`
- Test: `src/test/java/com/minispring/web/annotation/MappingAnnotationTest.java`

**Step 1: 创建RequestMapping注解**

```java
package com.minispring.web.annotation;

import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

/**
 * 请求映射注解
 * 用于将HTTP请求映射到处理器方法
 */
@Target({ElementType.TYPE, ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
public @interface RequestMapping {
    
    /**
     * 请求路径
     */
    String value() default "";
    
    /**
     * 请求方法
     */
    RequestMethod[] method() default {};
    
    /**
     * 请求方法枚举
     */
    enum RequestMethod {
        GET, POST, PUT, DELETE, PATCH, HEAD, OPTIONS, TRACE
    }
}
```

**Step 2: 创建GetMapping注解**

```java
package com.minispring.web.annotation;

import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

/**
 * GET请求映射注解
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@RequestMapping(method = RequestMapping.RequestMethod.GET)
public @interface GetMapping {
    
    /**
     * 请求路径
     */
    String value() default "";
}
```

**Step 3: 创建PostMapping注解**

```java
package com.minispring.web.annotation;

import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

/**
 * POST请求映射注解
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
@RequestMapping(method = RequestMapping.RequestMethod.POST)
public @interface PostMapping {
    
    /**
     * 请求路径
     */
    String value() default "";
}
```

**Step 4: 创建RequestParam注解**

```java
package com.minispring.web.annotation;

import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

/**
 * 请求参数绑定注解
 */
@Target(ElementType.PARAMETER)
@Retention(RetentionPolicy.RUNTIME)
public @interface RequestParam {
    
    /**
     * 参数名称
     */
    String value() default "";
    
    /**
     * 是否必需
     */
    boolean required() default true;
}
```

**Step 5: 编写测试**

**Step 6: 提交**

```bash
git add src/main/java/com/minispring/web/annotation/ src/test/java/com/minispring/web/annotation/
git commit -m "feat: 定义请求映射注解"
```

---

## 处理器映射

### Task 3: 实现HandlerMapping

**Files:**
- Create: `src/main/java/com/minispring/web/servlet/HandlerMapping.java`
- Create: `src/main/java/com/minispring/web/servlet/HandlerExecutionChain.java`
- Create: `src/main/java/com/minispring/web/servlet/RequestMappingHandlerMapping.java`
- Test: `src/test/java/com/minispring/web/servlet/HandlerMappingTest.java`

**Step 1: 创建HandlerMapping接口**

```java
package com.minispring.web.servlet;

import com.minispring.web.servlet.HttpServletRequest;
import com.minispring.web.servlet.HttpServletResponse;

/**
 * 处理器映射接口
 * 根据请求找到对应的处理器
 */
public interface HandlerMapping {
    
    /**
     * 根据请求获取处理器执行链
     *
     * @param request HTTP请求
     * @return 处理器执行链，如果没有找到返回null
     */
    HandlerExecutionChain getHandler(HttpServletRequest request) throws Exception;
}
```

**Step 2: 创建HandlerExecutionChain类**

```java
package com.minispring.web.servlet;

import java.util.ArrayList;
import java.util.List;

/**
 * 处理器执行链
 */
public class HandlerExecutionChain {
    
    private Object handler;
    private List<HandlerInterceptor> interceptors = new ArrayList<>();
    
    public HandlerExecutionChain(Object handler) {
        this.handler = handler;
    }
    
    /**
     * 获取处理器
     */
    public Object getHandler() {
        return handler;
    }
    
    /**
     * 添加拦截器
     */
    public void addInterceptor(HandlerInterceptor interceptor) {
        this.interceptors.add(interceptor);
    }
    
    /**
     * 获取所有拦截器
     */
    public List<HandlerInterceptor> getInterceptors() {
        return interceptors;
    }
}
```

**Step 3: 创建HandlerInterceptor接口**

```java
package com.minispring.web.servlet;

/**
 * 处理器拦截器接口
 */
public interface HandlerInterceptor {
    
    /**
     * 前置处理
     *
     * @return true表示继续执行，false表示中断
     */
    boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception;
    
    /**
     * 后置处理
     */
    void postHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception;
    
    /**
     * 完成后处理
     */
    void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex) throws Exception;
}
```

**Step 4: 创建RequestMappingHandlerMapping**

**Step 5: 提交**

```bash
git add src/main/java/com/minispring/web/servlet/
git commit -m "feat: 实现HandlerMapping"
```

---

## 前端控制器

### Task 4: 实现DispatcherServlet

**Files:**
- Create: `src/main/java/com/minispring/web/servlet/DispatcherServlet.java`
- Create: `src/main/java/com/minispring/web/servlet/WebApplicationContext.java`
- Test: `src/test/java/com/minispring/web/servlet/DispatcherServletTest.java`

**Step 1: 创建DispatcherServlet**

**Step 2: 创建WebApplicationContext**

**Step 3: 提交**

```bash
git add src/main/java/com/minispring/web/servlet/
git commit -m "feat: 实现DispatcherServlet前端控制器"
```

---

## 视图解析

### Task 5: 实现ViewResolver

**Files:**
- Create: `src/main/java/com/minispring/web/view/View.java`
- Create: `src/main/java/com/minispring/web/view/ViewResolver.java`
- Create: `src/main/java/com/minispring/web/view/InternalResourceView.java`
- Create: `src/main/java/com/minispring/web/view/InternalResourceViewResolver.java`
- Test: `src/test/java/com/minispring/web/view/ViewResolverTest.java`

**Step 1: 创建View接口**

**Step 2: 创建ViewResolver接口**

**Step 3: 创建InternalResourceView**

**Step 4: 创建InternalResourceViewResolver**

**Step 5: 提交**

```bash
git add src/main/java/com/minispring/web/view/
git commit -m "feat: 实现ViewResolver"
```

---

## 示例应用

### Task 6: 创建Web示例应用

**Files:**
- Create: `src/main/java/com/minispring/web/samples/UserController.java`
- Create: `src/main/webapp/WEB-INF/views/user/list.jsp`
- Create: `src/main/webapp/WEB-INF/web.xml`

**Step 1: 创建UserController**

**Step 2: 创建JSP视图**

**Step 3: 创建web.xml**

**Step 4: 提交**

```bash
git add src/main/java/com/minispring/web/samples/
git add src/main/webapp/
git commit -m "feat: 创建Web示例应用"
```

---

## 阶段6完成检查

### Task 7: 验证阶段6完成

**Files:**
- Create: `docs/phases/phase06-completion-checklist.md`

**Step 1: 创建完成检查清单**

**Step 2: 运行所有测试**

Run: `mvn test`
Expected: 所有测试通过

**Step 3: 创建阶段6标签**

```bash
git tag phase-6-completed
```

**Step 4: 提交完成文档**

```bash
git add docs/phases/phase06-completion-checklist.md
git commit -m "docs: 完成阶段6检查清单"
```

---

## 阶段6总结

完成本阶段后，你已经实现：
1. 完整的MVC框架
2. 前端控制器模式
3. 处理器映射机制
4. 视图解析功能
5. 注解驱动的控制器

**下一步：** 进入阶段7，实现高级特性
