# 异步（@Async）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `@Async` 接口方法在调用时提交到 Executor 异步执行，支持 `void` 与 `CompletableFuture` 返回，复用现有 AOP `addAdvisor` 机制，零容器代码改动。

**Architecture:** 新增 `com.minispring.async` 包：`@Async` 注解 + `AsyncInterceptor implements AroundAdvice`。用户 `container.addAdvisor(new DefaultAdvisor(asyncInterceptor, @Async 匹配器))` 接入；现有 `DefaultBeanContainer.applyAopProxy`（有 Advisor 时把接口 Bean 包成 JDK 代理）已提供代理路径，无需改容器。

**Tech Stack:** Java 17（`java.util.concurrent.Executor` / `CompletableFuture` / `Executors`，均为 JDK 自带）、JUnit 5、Maven。零新增第三方依赖。

## Global Constraints

- JDK 17+；`maven.compiler.source/target=17`。
- 核心容器**零第三方依赖**：本特性不得向 `pom.xml` 添加任何依赖（仅用 JDK `java.util.concurrent`）。
- 新增类型放 `com.minispring.async` 包；样例放 `com.minispring.samples.async`。
- **接口约束（强制）**：`@Async` 仅作用于**接口方法**（核心容器无真实 CGLIB，`CglibProxy` 实为 JDK 代理）。样例与集成测试一律用 `interface + impl`，Bean 按接口类型获取/使用。
- **零容器改动**：不改 `DefaultBeanContainer` / `ProxyFactory` / `CglibProxy` / `pom.xml` / 任何现有类；纯新增包。
- 全部代码注释、文档、提交信息使用中文；提交前缀 `feat(async): ...` / `docs: ...`。
- TDD：每个任务先写失败测试，再实现，再验证通过，最后提交。
- 容器通过反射实例化 Bean 时不对构造器调 setAccessible，故容器实例化的夹具/样例类必须 `public`。
- 提交卫生：本仓库跟踪 `.idea/`，IDE 可能自动 stage `.idea/*`——提交时用**显式 pathspec**（`git commit -- <files>`），只提交本任务的文件。
- plain `mvn` works now（settings.xml jdk-1.8 profile 已关闭，JDK 17 source 生效）。

---

## 文件结构

**新增（main）：**
- `src/main/java/com/minispring/async/Async.java` — `@Async` 注解（METHOD/RUNTIME）
- `src/main/java/com/minispring/async/AsyncInterceptor.java` — `AroundAdvice` 实现
- `src/main/java/com/minispring/samples/async/NotificationService.java` — 样例接口
- `src/main/java/com/minispring/samples/async/NotificationServiceImpl.java` — 样例实现
- `src/main/java/com/minispring/samples/async/AsyncDemo.java` — 入口

**新增（test）：**
- `src/test/java/com/minispring/async/AsyncInterceptorTest.java`
- `src/test/java/com/minispring/async/MailService.java` — 集成测试接口
- `src/test/java/com/minispring/async/MailServiceImpl.java` — 集成测试实现
- `src/test/java/com/minispring/async/AsyncContainerIntegrationTest.java`

**修改：**
- `README.md` — 结构树 / 运行命令 / 路线图 / 特性表

**不修改任何现有类**（`DefaultBeanContainer`、`ProxyFactory`、`pom.xml` 等一律不动）。

---

## Task 1: @Async 注解 + AsyncInterceptor（核心）

**Files:**
- Create: `src/main/java/com/minispring/async/Async.java`
- Create: `src/main/java/com/minispring/async/AsyncInterceptor.java`
- Test: `src/test/java/com/minispring/async/AsyncInterceptorTest.java`

**Interfaces:**
- Consumes: `com.minispring.aop.advice.AroundAdvice`（`Object around(MethodInvocation invocation) throws Throwable`）、`com.minispring.aop.advice.MethodInvocation`（`getMethod()` / `getArguments()` / `getTarget()` / `proceed()`）、JDK `java.util.concurrent.Executor` / `CompletableFuture` / `Executors`
- Produces:
  - `@Async` 注解（`@Target(METHOD) @Retention(RUNTIME)`）
  - `AsyncInterceptor implements AroundAdvice`：`AsyncInterceptor()` 默认守护线程缓存池；`AsyncInterceptor(Executor executor)`；`around`：非 `@Async` 方法直接 `proceed`；`void` 提交任务返回 null；`CompletableFuture` 提交任务返回完成后的 CF；其他返回类型抛 `IllegalStateException`

- [ ] **Step 1: 写失败测试**

`src/test/java/com/minispring/async/AsyncInterceptorTest.java`:

```java
package com.minispring.async;

import com.minispring.aop.advice.MethodInvocation;
import org.junit.jupiter.api.Test;

import java.lang.reflect.Method;
import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.ExecutionException;
import java.util.concurrent.Executor;

import static org.junit.jupiter.api.Assertions.*;

/**
 * AsyncInterceptor 单元测试：用捕获型 Executor（收集 Runnable，不内联执行）保证确定性
 */
public class AsyncInterceptorTest {

    /** 测试夹具：方法直接标 @Async，便于单元测试拦截器逻辑 */
    public static class Fixture {
        final List<String> log = new ArrayList<>();

        @Async
        public void doVoid(String tag) {
            log.add("void:" + tag);
        }

        @Async
        public CompletableFuture<String> doFuture(String tag) {
            log.add("future:" + tag);
            return CompletableFuture.completedFuture("R:" + tag);
        }

        @Async
        public CompletableFuture<String> doFutureFail() {
            throw new RuntimeException("boom");
        }

        @Async
        public String doBad() {
            return "x";
        }

        public String doSync() {
            return "sync";
        }
    }

    /** 构造一个包裹目标方法调用的 MethodInvocation */
    private MethodInvocation invocation(Object target, String name, Class<?>[] params, Object[] args) throws Exception {
        Method method = target.getClass().getMethod(name, params);
        return new MethodInvocation() {
            @Override public Method getMethod() { return method; }
            @Override public Object[] getArguments() { return args; }
            @Override public Object getTarget() { return target; }
            @Override public Object proceed() throws Throwable { return method.invoke(target, args); }
        };
    }

    private static final Class<?>[] STR = {String.class};

    @Test
    void voidAsyncShouldSubmitAndReturnNull() throws Exception {
        List<Runnable> captured = new ArrayList<>();
        AsyncInterceptor interceptor = new AsyncInterceptor(captured::add);
        Fixture fixture = new Fixture();

        Object result = interceptor.around(invocation(fixture, "doVoid", STR, new Object[]{"A"}));

        assertNull(result, "void 异步方法应返回 null");
        assertTrue(fixture.log.isEmpty(), "方法体不应在调用线程执行");
        assertEquals(1, captured.size(), "应提交 1 个任务到 Executor");
        captured.get(0).run();
        assertEquals(List.of("void:A"), fixture.log, "任务运行后才执行方法体");
    }

    @Test
    void completableFutureAsyncShouldCompleteAfterTaskRuns() throws Exception {
        List<Runnable> captured = new ArrayList<>();
        AsyncInterceptor interceptor = new AsyncInterceptor(captured::add);
        Fixture fixture = new Fixture();

        @SuppressWarnings("unchecked")
        CompletableFuture<String> future = (CompletableFuture<String>)
                interceptor.around(invocation(fixture, "doFuture", STR, new Object[]{"B"}));

        assertNotNull(future);
        assertFalse(future.isDone(), "任务未运行时 CF 不应完成");
        assertEquals(1, captured.size());
        captured.get(0).run();
        assertTrue(future.isDone());
        assertEquals("R:B", future.get());
    }

    @Test
    void completableFutureAsyncShouldCompleteExceptionallyOnTaskFailure() throws Exception {
        List<Runnable> captured = new ArrayList<>();
        AsyncInterceptor interceptor = new AsyncInterceptor(captured::add);
        Fixture fixture = new Fixture();

        @SuppressWarnings("unchecked")
        CompletableFuture<String> future = (CompletableFuture<String>)
                interceptor.around(invocation(fixture, "doFutureFail", new Class<?>[0], new Object[0]));

        captured.get(0).run();
        assertTrue(future.isCompletedExceptionally());
        ExecutionException ex = assertThrows(ExecutionException.class, future::get);
        assertEquals("boom", ex.getCause().getMessage());
    }

    @Test
    void unsupportedReturnTypeShouldThrow() throws Exception {
        List<Runnable> captured = new ArrayList<>();
        AsyncInterceptor interceptor = new AsyncInterceptor(captured::add);
        Fixture fixture = new Fixture();

        assertThrows(IllegalStateException.class,
            () -> interceptor.around(invocation(fixture, "doBad", new Class<?>[0], new Object[0])),
            "@Async 方法返回非 void/CompletableFuture 应抛 IllegalStateException");
    }

    @Test
    void nonAsyncMethodShouldProceedSynchronously() throws Exception {
        List<Runnable> captured = new ArrayList<>();
        AsyncInterceptor interceptor = new AsyncInterceptor(captured::add);
        Fixture fixture = new Fixture();

        Object result = interceptor.around(invocation(fixture, "doSync", new Class<?>[0], new Object[0]));

        assertEquals("sync", result, "无 @Async 的方法应直接同步 proceed");
        assertTrue(captured.isEmpty(), "非异步方法不应提交 Executor");
    }

    @Test
    void voidAsyncShouldSwallowTaskException() throws Exception {
        // void 任务的异常应被 printStackTrace 吞掉，不向外传播
        List<Runnable> captured = new ArrayList<>();
        AsyncInterceptor interceptor = new AsyncInterceptor(captured::add);
        Fixture fixture = new Fixture() {
            @Async
            public void doVoid(String tag) {
                throw new RuntimeException("void-boom");
            }
        };

        Object result = interceptor.around(invocation(fixture, "doVoid", STR, new Object[]{"X"}));
        assertNull(result);
        // 运行捕获的任务不应抛异常（被吞）
        assertDoesNotThrow(() -> captured.get(0).run());
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `mvn test -Dtest=AsyncInterceptorTest`
Expected: BUILD FAILURE —— 找不到符号 `Async` / `AsyncInterceptor`

- [ ] **Step 3: 实现**

`src/main/java/com/minispring/async/Async.java`:

```java
package com.minispring.async;

import java.lang.annotation.ElementType;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;

/**
 * 异步注解（方法级）
 * 标在接口方法上；Bean 经 AOP 代理后，调用该方法会被提交到 Executor 异步执行。
 */
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface Async {
}
```

`src/main/java/com/minispring/async/AsyncInterceptor.java`:

```java
package com.minispring.async;

import com.minispring.aop.advice.AroundAdvice;
import com.minispring.aop.advice.MethodInvocation;

import java.lang.reflect.Method;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.Executor;
import java.util.concurrent.Executors;

/**
 * 异步拦截器
 * 把 @Async 方法提交到 Executor 异步执行：
 * - void 返回：提交任务后立即返回 null（发后不管）；任务内异常 printStackTrace 不外传
 * - CompletableFuture 返回：提交任务，完成后 complete/completeExceptionally，返回该 future
 * - 其他返回类型：抛 IllegalStateException
 * 无 @Async 的方法：直接同步 proceed（防御性）
 */
public class AsyncInterceptor implements AroundAdvice {

    private final Executor executor;

    /**
     * 默认使用守护线程的缓存线程池（避免 demo/测试 JVM 挂起）
     */
    public AsyncInterceptor() {
        this(Executors.newCachedThreadPool(runnable -> {
            Thread thread = new Thread(runnable);
            thread.setDaemon(true);
            return thread;
        }));
    }

    /**
     * 自定义 Executor（测试可用捕获型/同步 Executor 保证确定性）
     */
    public AsyncInterceptor(Executor executor) {
        this.executor = executor;
    }

    @Override
    public Object around(MethodInvocation invocation) throws Throwable {
        Method method = invocation.getMethod();
        if (!method.isAnnotationPresent(Async.class)) {
            return invocation.proceed();
        }

        Class<?> returnType = method.getReturnType();
        if (returnType == void.class || returnType == Void.class) {
            executor.execute(() -> {
                try {
                    invocation.proceed();
                } catch (Throwable t) {
                    t.printStackTrace();
                }
            });
            return null;
        }

        if (CompletableFuture.class.isAssignableFrom(returnType)) {
            // 目标方法自身返回 CompletableFuture；把它的结果传播到外层 future
            CompletableFuture<Object> future = new CompletableFuture<>();
            executor.execute(() -> {
                try {
                    CompletableFuture<?> result = (CompletableFuture<?>) invocation.proceed();
                    result.whenComplete((value, error) -> {
                        if (error != null) {
                            future.completeExceptionally(error);
                        } else {
                            future.complete(value);
                        }
                    });
                } catch (Throwable t) {
                    future.completeExceptionally(t);
                }
            });
            return future;
        }

        throw new IllegalStateException(
                "@Async 方法必须返回 void 或 CompletableFuture: " + method);
    }
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `mvn test -Dtest=AsyncInterceptorTest`
Expected: BUILD SUCCESS，Tests run: 6

- [ ] **Step 5: 提交（显式 pathspec）**

```bash
git add src/main/java/com/minispring/async/Async.java \
        src/main/java/com/minispring/async/AsyncInterceptor.java \
        src/test/java/com/minispring/async/AsyncInterceptorTest.java
git commit -m "feat(async): 实现 @Async 注解与 AsyncInterceptor" -- \
        src/main/java/com/minispring/async/Async.java \
        src/main/java/com/minispring/async/AsyncInterceptor.java \
        src/test/java/com/minispring/async/AsyncInterceptorTest.java
```

---

## Task 2: 容器集成测试（addAdvisor + 接口代理）

**Files:**
- Create: `src/test/java/com/minispring/async/MailService.java`
- Create: `src/test/java/com/minispring/async/MailServiceImpl.java`
- Create: `src/test/java/com/minispring/async/AsyncContainerIntegrationTest.java`

**Interfaces:**
- Consumes: `AsyncInterceptor`、`@Async`（Task 1）；`DefaultBeanContainer.addAdvisor` / `registerBean` / `getBean`、`DefaultAdvisor`、`MethodMatcher`（现有）
- Produces: 验证 `addAdvisor(asyncInterceptor, @Async 匹配器)` 后，接口 Bean 的 @Async 方法经代理异步执行

- [ ] **Step 1: 写夹具与失败测试**

`src/test/java/com/minispring/async/MailService.java`:

```java
package com.minispring.async;

import java.util.concurrent.CompletableFuture;

/**
 * 集成测试用接口：@Async 标在接口方法上
 */
public interface MailService {

    @Async
    void send(String to);

    @Async
    CompletableFuture<String> fetch(String query);
}
```

`src/test/java/com/minispring/async/MailServiceImpl.java`（容器实例化，须 public）:

```java
package com.minispring.async;

import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.CompletableFuture;

/**
 * MailService 实现；用静态列表记录副作用便于测试断言
 */
public class MailServiceImpl implements MailService {

    public static final List<String> SENT = new ArrayList<>();

    @Override
    public void send(String to) {
        SENT.add(to);
    }

    @Override
    public CompletableFuture<String> fetch(String query) {
        return CompletableFuture.completedFuture("result:" + query);
    }
}
```

`src/test/java/com/minispring/async/AsyncContainerIntegrationTest.java`:

```java
package com.minispring.async;

import com.minispring.aop.DefaultAdvisor;
import com.minispring.aop.MethodMatcher;
import com.minispring.factory.DefaultBeanContainer;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.List;
import java.util.concurrent.CompletableFuture;

import static org.junit.jupiter.api.Assertions.*;

/**
 * @Async 与容器集成测试：addAdvisor + 接口代理 + 异步执行
 */
public class AsyncContainerIntegrationTest {

    @BeforeEach
    void clearSent() {
        MailServiceImpl.SENT.clear();
    }

    private DefaultBeanContainer containerWithAsync(List<Runnable> captured) {
        DefaultBeanContainer container = new DefaultBeanContainer();
        container.addAdvisor(new DefaultAdvisor(
                new AsyncInterceptor(captured::add),
                (MethodMatcher) (method, targetClass) -> method.isAnnotationPresent(Async.class)
        ));
        container.registerBean("mailService", MailServiceImpl.class);
        return container;
    }

    @Test
    void voidAsyncShouldRunViaProxyNotInline() {
        List<Runnable> captured = new ArrayList<>();
        MailService mail = (MailService) containerWithAsync(captured).getBean("mailService");

        mail.send("alice");

        assertTrue(MailServiceImpl.SENT.isEmpty(), "调用线程不应执行方法体（已提交到 Executor）");
        assertEquals(1, captured.size(), "代理应提交 1 个任务到 Executor");
        captured.get(0).run();
        assertEquals(List.of("alice"), MailServiceImpl.SENT);
    }

    @Test
    void completableFutureAsyncShouldReturnFutureViaProxy() throws Exception {
        List<Runnable> captured = new ArrayList<>();
        MailService mail = (MailService) containerWithAsync(captured).getBean("mailService");

        CompletableFuture<String> future = mail.fetch("q");

        assertNotNull(future);
        assertFalse(future.isDone());
        captured.get(0).run();
        assertTrue(future.isDone());
        assertEquals("result:q", future.get());
    }

    @Test
    void beanShouldBeProxied() {
        List<Runnable> captured = new ArrayList<>();
        Object bean = containerWithAsync(captured).getBean("mailService");
        // 代理对象不是原始实现类（JDK 代理）
        assertNotSame(MailServiceImpl.class, bean.getClass());
        assertInstanceOf(MailService.class, bean);
    }
}
```

- [ ] **Step 2: 运行测试，确认通过**

Run: `mvn test -Dtest=AsyncContainerIntegrationTest`
Expected: BUILD SUCCESS，Tests run: 3

> 说明：Task 1 已实现 AsyncInterceptor，本任务的容器路径（addAdvisor → applyAopProxy → JDK 代理 → @Async 匹配 → AsyncInterceptor）依赖既有 AOP 基础设施，故测试应直接通过（无需先红）。若失败，说明对既有 AOP 的假设有出入，按 BLOCKED 上报。

- [ ] **Step 3: 运行全量测试，确认无回归**

Run: `mvn test`
Expected: BUILD SUCCESS，全部测试通过（既有 AOP/事件/条件/国际化测试不受影响）

- [ ] **Step 4: 提交（显式 pathspec）**

```bash
git add src/test/java/com/minispring/async/MailService.java \
        src/test/java/com/minispring/async/MailServiceImpl.java \
        src/test/java/com/minispring/async/AsyncContainerIntegrationTest.java
git commit -m "test(async): 新增 @Async 容器集成测试" -- \
        src/test/java/com/minispring/async/MailService.java \
        src/test/java/com/minispring/async/MailServiceImpl.java \
        src/test/java/com/minispring/async/AsyncContainerIntegrationTest.java
```

---

## Task 3: 样例 AsyncDemo + README 更新

**Files:**
- Create: `src/main/java/com/minispring/samples/async/NotificationService.java`
- Create: `src/main/java/com/minispring/samples/async/NotificationServiceImpl.java`
- Create: `src/main/java/com/minispring/samples/async/AsyncDemo.java`
- Modify: `README.md`

**Interfaces:**
- Consumes: `@Async`、`AsyncInterceptor`、`DefaultAdvisor`、`MethodMatcher`、`DefaultBeanContainer`（Task 1-2 + 现有）
- Produces: 可运行的 `com.minispring.samples.async.AsyncDemo`

- [ ] **Step 1: 写样例代码**

`src/main/java/com/minispring/samples/async/NotificationService.java`:

```java
package com.minispring.samples.async;

import com.minispring.async.Async;

import java.util.concurrent.CompletableFuture;

/**
 * 通知服务接口：@Async 标在接口方法上
 */
public interface NotificationService {

    @Async
    void notify(String message);

    @Async
    CompletableFuture<String> prepare(String message);
}
```

`src/main/java/com/minispring/samples/async/NotificationServiceImpl.java`（容器实例化，须 public）:

```java
package com.minispring.samples.async;

import java.util.concurrent.CompletableFuture;

/**
 * 通知服务实现：方法体打印所在线程，便于观察异步
 */
public class NotificationServiceImpl implements NotificationService {

    @Override
    public void notify(String message) {
        System.out.println("[线程 " + Thread.currentThread().getName() + "] 发送通知: " + message);
    }

    @Override
    public CompletableFuture<String> prepare(String message) {
        System.out.println("[线程 " + Thread.currentThread().getName() + "] 准备通知: " + message);
        return CompletableFuture.completedFuture("已准备: " + message);
    }
}
```

`src/main/java/com/minispring/samples/async/AsyncDemo.java`:

```java
package com.minispring.samples.async;

import com.minispring.aop.DefaultAdvisor;
import com.minispring.aop.MethodMatcher;
import com.minispring.async.AsyncInterceptor;
import com.minispring.factory.DefaultBeanContainer;

import java.util.concurrent.CompletableFuture;

/**
 * 阶段7-4 - 异步示例
 * 演示：@Async 接口方法经 AOP 代理异步执行（void 发后不管 + CompletableFuture 返回结果）
 */
public class AsyncDemo {

    public static void main(String[] args) throws Exception {
        System.out.println("=== 阶段7-4：异步 @Async 示例 ===\n");
        System.out.println("[main 线程: " + Thread.currentThread().getName() + "]");

        DefaultBeanContainer container = new DefaultBeanContainer();
        // 接入 @Async：AsyncInterceptor + 匹配 @Async 方法的 MethodMatcher
        container.addAdvisor(new DefaultAdvisor(
                new AsyncInterceptor(),
                (MethodMatcher) (method, targetClass) -> method.isAnnotationPresent(com.minispring.async.Async.class)
        ));
        container.registerBean("notificationService", NotificationServiceImpl.class);

        NotificationService service = (NotificationService) container.getBean("notificationService");

        System.out.println("\n--- 调用 notify（void，发后不管）---");
        service.notify("hello");
        System.out.println("[main] notify 已返回（方法体在另一线程执行）");

        System.out.println("\n--- 调用 prepare（CompletableFuture，等待结果）---");
        CompletableFuture<String> future = service.prepare("world");
        System.out.println("[main] prepare 已返回 future，等待结果...");
        String result = future.get();
        System.out.println("[main] prepare 结果: " + result);

        System.out.println("\n=== 阶段7-4 示例结束 ===");
    }
}
```

- [ ] **Step 2: 编译并运行样例**

Run: `mvn compile exec:java -Dexec.mainClass="com.minispring.samples.async.AsyncDemo"`
Expected: 输出包含 `notify` 与 `prepare` 的方法体在**非 main 线程**（如 `Thread-0` / `pool-1-thread-1`）执行，且 `prepare 结果: 已准备: world`。main 线程的 `[main] notify 已返回` 在 notify 方法体打印前后均可（体现异步）。

- [ ] **Step 3: 更新 README**

**(a) 特性概览表** —— 在 7-3 行之后追加 7-4 行。将：
```
| 7-3 | 国际化 | `MessageSource`、`ResourceBundleMessageSource`/`StaticMessageSource`、`MessageFormat` 参数替换、`@Autowired` 注入 |
```
改为（在其后新增一行）：
```
| 7-3 | 国际化 | `MessageSource`、`ResourceBundleMessageSource`/`StaticMessageSource`、`MessageFormat` 参数替换、`@Autowired` 注入 |
| 7-4 | 异步 | `@Async`（接口方法）、`AsyncInterceptor`（`AroundAdvice`）、`Executor` 异步执行、`void`/`CompletableFuture` 返回 |
```

**(b) 项目结构 —— 顶层包区** —— 在 `i18n/` 块之后、`samples/` 之前插入 `async/` 块。将：
```
│   │   └── StaticMessageSource / ResourceBundleMessageSource
│   └── samples/                  # 各阶段示例（按阶段分包，每个含一个 *Demo 入口）
```
改为：
```
│   │   └── StaticMessageSource / ResourceBundleMessageSource
│   ├── async/                    # 异步（阶段 7-4）
│   │   ├── Async（注解）
│   │   └── AsyncInterceptor（AroundAdvice，提交 Executor）
│   └── samples/                  # 各阶段示例（按阶段分包，每个含一个 *Demo 入口）
```

**(c) 项目结构 —— samples 区** —— 在 `messagesource/` 之后追加 `async/`。将：
```
│       └── messagesource/                # 阶段7-3 MessageSourceDemo（GreetingService）
```
改为：
```
│       ├── messagesource/                # 阶段7-3 MessageSourceDemo（GreetingService）
│       └── async/                        # 阶段7-4 AsyncDemo（NotificationService）
```

**(d) 运行示例命令** —— 在阶段7-3 命令之后追加。将：
```
# 阶段7-3 国际化
mvn compile exec:java -Dexec.mainClass="com.minispring.samples.messagesource.MessageSourceDemo"
```
改为：
```
# 阶段7-3 国际化
mvn compile exec:java -Dexec.mainClass="com.minispring.samples.messagesource.MessageSourceDemo"

# 阶段7-4 异步
mvn compile exec:java -Dexec.mainClass="com.minispring.samples.async.AsyncDemo"
```

**(e) 路线图表** —— 在 7-3 行之后插入 7-4 行。将：
```
| 7-3 | 国际化（MessageSource / 多 locale / 参数替换） | ✅ 已完成 |
| 7 | 异步 `@Async` → 事务 `@Transactional` | ⏳ 计划中（按此顺序逐个推进） |
```
改为：
```
| 7-3 | 国际化（MessageSource / 多 locale / 参数替换） | ✅ 已完成 |
| 7-4 | 异步（@Async / AsyncInterceptor） | ✅ 已完成 |
| 7 | 事务 `@Transactional` | ⏳ 计划中 |
```

- [ ] **Step 4: 运行全部测试，确认无回归**

Run: `mvn test`
Expected: BUILD SUCCESS，全部测试通过

- [ ] **Step 5: 提交（显式 pathspec）**

```bash
git add src/main/java/com/minispring/samples/async/NotificationService.java \
        src/main/java/com/minispring/samples/async/NotificationServiceImpl.java \
        src/main/java/com/minispring/samples/async/AsyncDemo.java \
        README.md
git commit -m "feat(async): 新增阶段7-4 异步样例 AsyncDemo 并更新 README" -- \
        src/main/java/com/minispring/samples/async/NotificationService.java \
        src/main/java/com/minispring/samples/async/NotificationServiceImpl.java \
        src/main/java/com/minispring/samples/async/AsyncDemo.java \
        README.md
```

---

## 完成标准

- 全部 3 个任务提交，`mvn test` 全绿（含 Task 1-2 新增测试，无既有测试回归）。
- `AsyncDemo` 可运行：`notify`/`prepare` 方法体在非 main 线程执行；`prepare` 经 `future.get()` 取到 `已准备: world`。
- `AsyncInterceptor` 正确处理 void（提交返回 null）/ CompletableFuture（提交返回完成后的 CF）/ 其他返回类型（抛 IllegalStateException）/ 非 @Async（同步 proceed）。
- 未引入任何新的第三方依赖；未改动 `DefaultBeanContainer` / `ProxyFactory` / `pom.xml` / 任何现有类。
